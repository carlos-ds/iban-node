name: Trigger test run with Jest

on:
  push:
    branches:
      - develop

jobs:
  test:
    name: Jest
    runs-on: ubuntu-latest
    steps: 
      - name: Test with Jest
        uses: actions/checkout@v2
        run: npm test
